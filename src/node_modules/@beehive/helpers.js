export function checkSearch(searchValue, record) {
	if (searchValue.trim().length === 0) return true; // Ignore when not searching

	const searchString = searchValue.toLowerCase();
	const name = record.name.toLowerCase();
	const description = record.description.toLowerCase();

	return (name.indexOf(searchString) !== -1 || description.indexOf(searchString) !== -1);
}