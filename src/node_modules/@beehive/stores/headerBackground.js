import { writable } from 'svelte/store';

function createHeaderBackground() {
	const headerBackground = writable(null);

	return {
		subscribe: headerBackground.subscribe,

		setColor(color) {
			headerBackground.set(color);
		},

		reset() {
			headerBackground.set(null);
		},
	};
}

export const headerBackground = createHeaderBackground();